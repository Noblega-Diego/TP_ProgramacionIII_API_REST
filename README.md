# TP_ProgramacionIII_API_REST
 API-REST-agenda with SPRING BOOT

### Guie
  
  1) install MySql  
  
  2) Create a database called: restdb
 
  3) Compile and run
     : use port 8080
  
routes:

  GET: api/contactos/ 
  
  GET: api/contactos/{id}
  
  POST: api/contactos
  
  PUT: api/contactos/{id}
  
  DELETE: api/contactos/{id}
